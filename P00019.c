#include <stdio.h>

int main()
{
    int n;
    for(; ;)
        {
            scanf("%d", &n);
            if(n!=19)
                continue;
            if(n=19)
                break;
        }
    printf("%d", n);
}
